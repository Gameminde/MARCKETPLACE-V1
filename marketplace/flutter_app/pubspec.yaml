name: marketplace_app
description: Une marketplace moderne et fluide

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Navigation
  cupertino_icons: ^1.0.2
  
  # HTTP & API
  http: ^0.13.5
  dio: ^4.0.6
  
  # State Management
  provider: ^6.0.5
  
  # Storage & Persistence
  shared_preferences: ^2.0.18
  
  # Authentication & Security
  crypto: ^3.0.2
  
  # Images & Media
  cached_network_image: ^3.2.3
  
  # Animations & Effects
  shimmer: ^2.0.0
  
  # UI Enhancements
  flutter_rating_bar: ^4.0.1
  
  # Utilities & Helpers
  intl: ^0.18.1
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
